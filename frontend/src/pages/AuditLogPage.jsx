import AuditLog from '../components/AuditLog'

/** AuditLogPage wraps the AuditLog component with a heading. */
export default function AuditLogPage() {
  return (
    <div>
      <h2>Audit Log</h2>
      <AuditLog />
    </div>
  )
}
